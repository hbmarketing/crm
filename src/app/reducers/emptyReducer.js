export default function reducer(state, action) {
    switch (action.type) {
        case "FETCH_some_PENDING":
            break;
        case "FETCH_some_FULFILLED":
            break;
        case "FETCH_some_REJECTED":
            break;
    }
}